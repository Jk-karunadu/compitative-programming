#include <iostream>

using namespace std;

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    int t_cases; 
    cin >> t_cases;

    while (t_cases--)
    {
        long long a, b, c, d;
        cin >> a >> b >> c >> d;

        cout << b << " " << c << " " << c << "\n";
    }

    return 0;
}